---
description: Manager SSR removido — ver frontend + API
globs: manager/**/*
alwaysApply: false
---

# Manager (legado)

O painel SSR foi **removido na Fase 6**. Resta apenas `manager/models/` — dados consumidos pela API REST.

Substituto: `frontend/` + `backend/api/`. Ver `.cursor/rules/api.mdc` e `.cursor/docs/frontend.md`.
